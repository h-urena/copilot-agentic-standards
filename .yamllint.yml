extends: relaxed
rules:
  line-length:
    max: 140  # 40-char SHA-pinned `uses:` lines can reach ~130 chars; 140 gives safe headroom
    level: error
  new-lines: disable  # Windows uses CRLF; git handles conversion
