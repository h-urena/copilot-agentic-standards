extends: relaxed
rules:
  line-length:
    max: 130  # SHA-pinned `uses:` lines peak at 130 chars (pr-description.yml); no extra headroom needed
    level: error
  new-lines: disable  # Windows uses CRLF; git handles conversion
