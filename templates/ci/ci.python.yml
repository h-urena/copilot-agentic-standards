# CI pipeline template for Python projects.
# Copy to .github/workflows/ci.yml in your project.
#
# NOTE: The reusable workflow references below point to h-urena/copilot-agentic-standards.
# If you have forked or mirrored the standards repo, update the org/repo in every `uses:` line.

name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  issues: write

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── Governance checks (reusable from standards repo) ──
  merge-rules:
    if: github.event_name == 'pull_request'
    uses: h-urena/copilot-agentic-standards/.github/workflows/merge-rules.yml@main
    permissions:
      contents: read
      pull-requests: read

  pr-description:
    if: github.event_name == 'pull_request'
    uses: h-urena/copilot-agentic-standards/.github/workflows/pr-description.yml@main
    permissions:
      pull-requests: write

  pr-automation:
    if: github.event_name == 'pull_request'
    uses: h-urena/copilot-agentic-standards/.github/workflows/pr-automation.yml@main
    permissions:
      contents: read
      pull-requests: write
      issues: write
    with:
      enable-labeler: true

  code-review:
    if: github.event_name == 'pull_request'
    uses: h-urena/copilot-agentic-standards/.github/workflows/code-review.yml@main
    permissions:
      contents: read
      pull-requests: write

  # ── Lint & Type Check ──
  lint:
    name: Enforce code style and type safety
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up uv package manager
        uses: astral-sh/setup-uv@v4

      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Install locked dependencies
        run: uv sync --frozen

      - name: Enforce Ruff code style
        run: uv run ruff check .

      - name: Enforce Ruff formatting
        run: uv run ruff format --check .

      - name: Enforce strict mypy type checking
        run: uv run mypy --strict .

  # ── Unit tests ──
  test-unit:
    name: Unit tests pass
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v6

      - name: Set up uv package manager
        uses: astral-sh/setup-uv@v4

      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Install locked dependencies
        run: uv sync --frozen

      - name: Run unit tests with coverage
        run: uv run pytest tests/unit -v --cov --cov-report=xml

      - name: Persist coverage report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-unit
          path: coverage.xml

  # ── Integration tests ──
  test-integration:
    name: Integration tests pass
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v6

      - name: Set up uv package manager
        uses: astral-sh/setup-uv@v4

      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Install locked dependencies
        run: uv sync --frozen

      - name: Run integration test suite
        run: uv run pytest tests/integration -v

  # ── Build ──
  build:
    name: Application compiles and packages
    runs-on: ubuntu-latest
    needs: [test-unit, test-integration]
    steps:
      - uses: actions/checkout@v6

      - name: Set up uv package manager
        uses: astral-sh/setup-uv@v4

      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Build distributable package
        run: uv build

      - name: Persist build artifact
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/

  # ── Docker (optional — uncomment if using containers) ──
  # docker:
  #   name: Docker Build
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - uses: actions/checkout@v6
  #
  #     - name: Build Docker image
  #       run: docker build -t app:${{ github.sha }} .
  #
  #     - name: Run container health check
  #       run: |
  #         docker run -d --name app -p 8000:8000 app:${{ github.sha }}
  #         sleep 5
  #         curl -f http://localhost:8000/health || exit 1
  #         docker stop app
