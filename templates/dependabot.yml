version: 2

updates:
  # Keep GitHub Actions up to date automatically.
  # versioning-strategy is not applicable to github-actions (Dependabot always pins to the latest
  # available SHA/tag for the major version). For package ecosystems below, set
  # versioning-strategy to "increase" (bump minimum) or "lockfile-only" (only update lockfile).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "UTC"
    labels:
      - "chore"
      - "dependencies"
    commit-message:
      prefix: "chore(ci)"
    open-pull-requests-limit: 5
    groups:
      github-actions:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"

  # Uncomment and adjust for your stack:

  # npm / pnpm
  # - package-ecosystem: "npm"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
  #     day: "monday"
  #   versioning-strategy: "lockfile-only"  # lockfile is source of truth; no package.json range changes
  #   commit-message:
  #     prefix: "chore(deps)"
  #   open-pull-requests-limit: 10
  #   groups:
  #     production-deps:
  #       dependency-type: "production"
  #       update-types: ["minor", "patch"]  # major bumps get individual PRs
  #     dev-deps:
  #       dependency-type: "development"
  #       update-types: ["minor", "patch"]

  # pip / uv / poetry
  # - package-ecosystem: "pip"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
  #     day: "monday"
  #   versioning-strategy: "lockfile-only"  # lockfile is source of truth; no pyproject.toml range changes
  #   commit-message:
  #     prefix: "chore(deps)"
  #   open-pull-requests-limit: 10

  # nuget
  # - package-ecosystem: "nuget"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
  #     day: "monday"
  #   versioning-strategy: "lockfile-only"  # lockfile is source of truth; no .csproj range changes
  #   commit-message:
  #     prefix: "chore(deps)"
  #   open-pull-requests-limit: 10
